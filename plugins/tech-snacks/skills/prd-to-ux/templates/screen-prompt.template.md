# {{feature_name}} — Screen Prompts

_Each block below is self-contained. Copy one block, paste into Stitch / Figma AI / Pencil / Claude Design._

---

## {{screen_name}} — {{state_name}}

````
**What this screen is for:**
{{one sentence — the user's goal on this screen in this state}}

**What's visible:**
{{content, hierarchy, key elements in natural language — no component trees, no class names}}

**What the user can do:**
- Primary: {{primary action}}
- Secondary: {{secondary actions, if any}}

**Feel:**
{{pulled from 03-design-direction.md if present, else shadcn-default neutrals — describe, do not prescribe classes}}

**State context:**
{{e.g., "this is the empty state — user has no notes yet"}}

**Critical affordances:**
{{anything that must survive the rendering tool's interpretation — e.g., "the capture input should feel like a fresh page, not a form field"}}
````

---

(Repeat one "## {{screen_name}} — {{state_name}}" block per screen × state.)
